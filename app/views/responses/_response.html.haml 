- user = User.find(response.from_user_id)
.user.col-sm-6
  - if user.img.attachment.present? && user.profile_pic_visible?
    .image
      = image_tag(user.img)
  - else
  - if user.handle_visible?
    .text-center= user.email
  - if user.location_visible?
    - if user.lat.present?
      .map-link{data:{addr: "#{@response.user.lat},#{@response.user.lngt}"}}
        = image_tag('map.png', class: 'map-ico')
  %h2 answers
    %strong= user.question_1
    .clearfix
    = response.answer_1
    %br
    %strong= user.question_2
    .clearfix
    = response.answer_2
    %br
    %strong= user.question_3
    .clearfix
    = response.answer_3
    %br